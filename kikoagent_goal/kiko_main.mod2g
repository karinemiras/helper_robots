use facts as knowledge.
use dynamics as knowledge.
use fixed_goals as goals.

use kiko_actions as actionspec.

use watch_for_subjects as module.

order=linearall.
%exit=always.
 
module kiko_main 
{
 
   if goal(helping), bel(not(watching)) then watch_for_subjects.


}