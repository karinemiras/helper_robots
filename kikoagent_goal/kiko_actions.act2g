use facts as knowledge.
use dynamics as knowledge.

define sayAnimated(Text) with
	pre { true }
	post { true }
 
define startWatching with
	pre { not(watching) }
	post { watching } 
		
define stopWatching with
	pre { true }
	post { true }
	
define startListening with
	pre { not(listening) }
	post { listening }

define stopListening with
	pre { listening }
	post {  not(listening) }
	
define startListening(Context) with
	pre { not(listening) }
	post { listening }
	
define stopListening(Context) with
	pre { listening }
	post { not(listening) }
	
	